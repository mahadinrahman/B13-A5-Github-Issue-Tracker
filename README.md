1.what is the difference between var,letand const?
=>
-----var---------
-it is an old way of declaring variables
-function is scoped
-it can be redeclared
-it can be updated
-gets hoisted with undefined

------let---------------
-It is  block scoped
-it cannot be redeclared
-can be updated

---------const------------
-used when the value should not change
-it is block scoped
-it can not be redeclared
-it can not be updated

2.What is the spread operator (...)?
=>
The spread operator is used to expand elements of an array or properties of an object into another array or object.It spreads or copies the elements of an array or object.

**the uses of spread operator
-copy arrays
-merge arrays
-copy objects
-update objects without modifiying the orginal data


3.What is the difference between map(), filter(), and forEach()?
=>
*********map()**************
map() creats a new array by applying a function to each element.

*******filter()***************
filter() creates a new array with elements that match a condition.

*************forEach()***********
forEach() loops through an array but does not return anything.

4. What is an arrow function?
=>
An arrow function is a shorter and modern way to write functions.
-It is uses arrow simble.
-It is a short symble of writing functions
-makes code cleaner and easier to read
-commonly used with array methods




